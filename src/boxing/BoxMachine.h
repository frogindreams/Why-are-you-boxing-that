#include <vector>
#include <iostream>

#ifndef BOXMACHINE_H
#define BOXMACHINE_H

std::vector<int> vain_sequence;

void packSequence(vector<int> cut_q, int size_of_seq) {
    /*
    vain_sequnce(size_of_seq, 0);
    for (elem : vain_sequence) {
        std::cout << elem << '\n';
    }
    */

    std::cout << "what the heck" << '\n';
}

#endif
